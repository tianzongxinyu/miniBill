services:
  minibill:
    build: .
    ports:
      - "8080:8080"
    environment:
      ALLOW_REGISTRATION: ${ALLOW_REGISTRATION:-true}
      DATA_DIR: /data
      BACKUP_DIR: ${BACKUP_DIR:-/data/backups}
      JWT_EXPIRE_DAYS: "30"
      TZ: Asia/Shanghai
    volumes:
      - ./data:/data
    restart: unless-stopped
